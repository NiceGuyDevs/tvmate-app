/// Template-matched category / chip row height (Live TV, Movies, Series, detail
/// actions). Category label is ~12.5pt; this is the outer [SizedBox] height so the
/// pill is only slightly taller than the line box.
const double kTvTemplateCategoryPillHeight = 24.0;

/// Height of the horizontal category strip row (Live TV uses this; Movies /
/// Series match for pixel parity). Slightly above [kTvTemplateCategoryPillHeight]
/// for layout slack.
const double kTvTemplateCategoryStripRowHeight = 36.0;
