/// Built-in OpenSubtitles.com REST API key (shipped with the app).
///
/// **Manual change (you edit source, then run your own build):**
/// 1. Open this file in the project:
///    `lib/data/opensubtitles_config.dart`
/// 2. Replace the string value of [kOpenSubtitlesBuiltInApiKey] below (lines with
///    the quoted token) — keep the quotes.
/// 3. Rebuild the app (`flutter build apk` or your usual command).
/// The store always uses this constant; there is no in-app API key field.
const String kOpenSubtitlesBuiltInApiKey =
    'FhgV7unusL2AKZVLOXUksYDtLOSWnhIx';
