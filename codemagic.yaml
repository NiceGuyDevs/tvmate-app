workflows:
  ios-release:
    name: iOS Release Build

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - name: Install deps
        script: flutter pub get

      - name: Build (USE EXISTING SIGNING)
        script: |
          flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa
